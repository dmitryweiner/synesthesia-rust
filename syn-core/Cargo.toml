[package]
name = "syn-core"
description = "Pure model: formula generators, modulation, FX, state, genome. No I/O."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
rayon = "1"

[lints]
workspace = true
